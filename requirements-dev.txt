# HOC - Dependencias de desarrollo, tests y benchmarks
# Phase 3 (2026-04-23): versiones pinneadas para CI reproducible.

-r requirements.txt

# Testing
# pytest 8.4.1 / black 25.1.0 were bumped 2026-04-24 to fix CVEs
# GHSA-6w46-j5rx-g56g (pytest) and GHSA-3936-cmfr-pm3m (black) surfaced by
# pip-audit in CI.
pytest==9.0.3
# Phase 7.1: pytest-asyncio for the async tick migration. Pinned to
# 1.1.0 to match what landed Phase 7's CI runs.
pytest-asyncio==1.1.0
pytest-benchmark==5.1.0
pytest-cov==6.1.1
pytest-mock==3.15.1
pytest-timeout==2.4.0
hypothesis==6.151.11

# Linting / formatting / types
ruff==0.8.6
black==26.3.1
mypy==1.14.1
pre-commit==4.0.1

# Security / supply chain
bandit==1.8.0
pip-audit==2.9.0

# Complexity / static analysis
radon==6.0.1
